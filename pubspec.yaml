name: photo_editor_test
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.2.6
  flutter: 3.16.9

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  photo_editor_sdk: ^3.0.0
  video_editor_sdk: ^3.0.0
  imgly_sdk: ^3.0.0

  local_auth: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/
